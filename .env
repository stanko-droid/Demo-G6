# Demo-G6 Environment Configuration
# For production, these should be set in Azure Container Apps secrets

# Load database URL from .database-url file
DATABASE_URL=$(cat .database-url)

# Load secret key from .secret-key file
SECRET_KEY=$(cat .secret-key)

# Flask configuration
FLASK_ENV=production
FLASK_APP=app:app

# Database configuration
SQLALCHEMY_DATABASE_URI=${DATABASE_URL}
SQLALCHEMY_TRACK_MODIFICATIONS=false

# Application
DEBUG=false
